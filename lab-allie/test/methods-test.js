'use strict';

const assert = require('assert');

const methods = require('../lib/methods.js');

describe('methods.js', function() {
  describe('sandwichOrder()', function() {

  });
});

// it('should return a string with the customer order', function() {
//   assert.equal(order.sandwichOrder('name', 'meat', 'bread', 'cheese'), `${name} would like a ${meat} sandwich on ${bread} with ${cheese}.`);
// });